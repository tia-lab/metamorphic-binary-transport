# Rust tooling validation — 2026-09-19

Local development validation; no new performance measurements or remote CI run.

| Command | Result | Log |
| --- | --- | --- |
| `cargo test --workspace --all-features` | 178 passed, zero failed | [tests](tests.log) |
| `cargo check --workspace --all-targets --all-features` | Passed | [check](check.log) |
| `cargo build --workspace --all-features` | Passed | [build](build.log) |
| `cargo fmt --all -- --check` | Passed | [format](format.log) |
| `cargo run -p mbt_codegen --bin schema_codegen -- --write` | Passed; 24 generated files unchanged | [write](generation-write.log) |
| `cargo run -p mbt_codegen --bin schema_codegen -- --check` | Passed | [check](generation-check.log) |
| `cargo run -p mbt_codegen --bin schema_codegen -- --inspect` | Passed | [inspect](generation-inspect.log) |
| `cargo check --manifest-path /tmp/mbt-readme-check/Cargo.toml --offline` | All nine README Rust blocks compile as separate modules | [examples](readme-examples.log) |
| `git diff --check` | Passed | [diff](diff.log) |

The README example check used a temporary Rust 2024 library with local dependencies
on core, compression, metamorphose and measurement (json/protobuf/csv/arrow_ipc).
It compiled the displayed examples; it did not execute their function bodies.

CSV/JSON oracle tests now use the Rust `csv` and `serde_json` readers; protobuf
oracle tests use independently declared test-only `prost` message types. Fixtures
retain absent versus present-zero values, timestamps, dictionaries, arrays, UTF-8,
quotes, backslashes and control characters. Reader dependencies are dev-dependencies.

[Generation hashes](generated_sha256.json) record unchanged generated bytes.
[Command records](commands.json) include exit codes and timings for generation/build.
[Environment and source identity](environment.json) bind this validation to its inputs.
