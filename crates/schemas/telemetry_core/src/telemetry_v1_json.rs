// This file is @generated by mbt_codegen.
// Do not edit by hand.
// schema_id=50001 schema_version=1 schema_hash=232196938863914698

use crate::telemetry_v1::*;
use mbt_adapter_json::JsonWriter;
use mbt_core::error::Result;
use mbt_metamorphose::{JsonMetamorphoseSchema, runtime::TrustedUnchecked};
use rkyv::Archive;

const JSON_SCHEMA_VERSION_FIELD: &[u8] = b"\"schema_version\":";
const JSON_ROWS_FIELD: &[u8] = b"\"rows\":";
const JSON_FIELD_SCHEMA_VERSION: &[u8] = b"\"schema_version\":";
const JSON_FIELD_DEVICE_ORDINAL: &[u8] = b"\"device\":";
const JSON_FIELD_RECORDED_AT_MS: &[u8] = b"\"recorded_at_ms\":";
const JSON_FIELD_RECORDED_AT_UTC: &[u8] = b"\"recorded_at_utc\":";
const JSON_FIELD_TEMPERATURE_C: &[u8] = b"\"temperature_c\":";
const JSON_FIELD_BATTERY_PERCENT: &[u8] = b"\"battery_percent\":";
const JSON_FIELD_STATUS_ORDINAL: &[u8] = b"\"status\":";
const JSON_FIELD_TAGS_MASK: &[u8] = b"\"tags\":";

impl TelemetryV1 {
    pub fn metamorphose_json(bytes: &[u8], max_response_bytes: usize) -> Result<Vec<u8>> {
        let archived = Self::access_archived(bytes)?;
        write_json_response(archived, max_response_bytes)
    }

    /// Metamorphoses immutable bytes already validated for this schema into JSON.
    ///
    /// # Safety
    /// The caller guarantees checked schema validation happened before immutable storage or transport.
    pub unsafe fn metamorphose_json_trusted_unchecked(
        bytes: &[u8],
        max_response_bytes: usize,
    ) -> Result<Vec<u8>> {
        let archived = unsafe { Self::access_archived_trusted_unchecked(bytes)? };
        write_json_response(archived, max_response_bytes)
    }
}

impl JsonMetamorphoseSchema for TelemetryV1 {
    fn metamorphose_json(bytes: &[u8], max_response_bytes: usize) -> Result<Vec<u8>> {
        Self::metamorphose_json(bytes, max_response_bytes)
    }
    fn metamorphose_json_trusted_unchecked(
        bytes: &[u8],
        max_response_bytes: usize,
        _trusted: TrustedUnchecked,
    ) -> Result<Vec<u8>> {
        unsafe { Self::metamorphose_json_trusted_unchecked(bytes, max_response_bytes) }
    }
}

fn write_json_response(
    archived: &ArchivedTelemetryResponseV1Payload,
    max_response_bytes: usize,
) -> Result<Vec<u8>> {
    let mut writer = JsonWriter::with_capacity(max_response_bytes, max_response_bytes.min(4096));
    writer.begin_object()?;
    writer.raw_static(JSON_SCHEMA_VERSION_FIELD)?;
    writer.u32_value(u32::from(SCHEMA_VERSION_VALUE))?;
    writer.comma()?;
    writer.raw_static(JSON_ROWS_FIELD)?;
    writer.begin_array()?;
    let mut first_row = true;
    for row in archived.rows.iter() {
        if first_row {
            first_row = false;
        } else {
            writer.comma()?;
        }
        write_json_row(row, &mut writer)?;
    }
    writer.end_array()?;
    writer.end_object()?;
    Ok(writer.finish())
}

fn write_json_row(
    row: &<TelemetryRowV1 as Archive>::Archived,
    writer: &mut JsonWriter,
) -> Result<()> {
    writer.begin_object()?;
    let mut first = true;
    write_json_field_prefix(writer, &mut first, JSON_FIELD_SCHEMA_VERSION)?;
    writer.u32_value(u32::from(1_u16))?;
    write_json_field_prefix(writer, &mut first, JSON_FIELD_DEVICE_ORDINAL)?;
    writer.string_value(device_symbol(row.device_ordinal.to_native())?)?;
    write_json_field_prefix(writer, &mut first, JSON_FIELD_RECORDED_AT_MS)?;
    writer.i64_value(row.recorded_at_ms.to_native())?;
    write_json_field_prefix(writer, &mut first, JSON_FIELD_RECORDED_AT_UTC)?;
    writer.utc_value(row.recorded_at_ms.to_native())?;
    write_json_field_prefix(writer, &mut first, JSON_FIELD_TEMPERATURE_C)?;
    writer.f64_value("temperature_c", row.temperature_c.to_native())?;
    if row.presence_bits.to_native() & PRESENCE_BATTERY_PERCENT != 0 {
        write_json_field_prefix(writer, &mut first, JSON_FIELD_BATTERY_PERCENT)?;
        writer.f64_value("battery_percent", row.battery_percent.to_native())?;
    }
    write_json_field_prefix(writer, &mut first, JSON_FIELD_STATUS_ORDINAL)?;
    writer.string_value(status_symbol(row.status_ordinal.to_native())?)?;
    write_json_field_prefix(writer, &mut first, JSON_FIELD_TAGS_MASK)?;
    write_json_tag_bitmask(row.tags_mask.to_native(), writer)?;
    writer.end_object()
}

fn write_json_tag_bitmask(mask: u64, writer: &mut JsonWriter) -> Result<()> {
    writer.begin_array()?;
    let mut first = true;
    if mask & (1_u64 << 0) != 0 {
        if first {
            first = false;
        } else {
            writer.comma()?;
        }
        writer.string_value("indoor")?;
    }
    if mask & (1_u64 << 1) != 0 {
        if first {
            first = false;
        } else {
            writer.comma()?;
        }
        writer.string_value("outdoor")?;
    }
    if mask & (1_u64 << 2) != 0 {
        if first {
            first = false;
        } else {
            writer.comma()?;
        }
        writer.string_value("test")?;
    }
    let _ = first;
    writer.end_array()
}

fn write_json_field_prefix(
    writer: &mut JsonWriter,
    first: &mut bool,
    field: &'static [u8],
) -> Result<()> {
    if *first {
        *first = false;
    } else {
        writer.comma()?;
    }
    writer.raw_static(field)
}

const TEMPERATURE_ONLY_JSON_SCHEMA_VERSION_FIELD: &[u8] = b"\"schema_version\":";
const TEMPERATURE_ONLY_JSON_ROWS_FIELD: &[u8] = b"\"rows\":";
const TEMPERATURE_ONLY_JSON_FIELD_SCHEMA_VERSION: &[u8] = b"\"schema_version\":";
const TEMPERATURE_ONLY_JSON_FIELD_DEVICE_ORDINAL: &[u8] = b"\"device\":";
const TEMPERATURE_ONLY_JSON_FIELD_RECORDED_AT_MS: &[u8] = b"\"recorded_at_ms\":";
const TEMPERATURE_ONLY_JSON_FIELD_RECORDED_AT_UTC: &[u8] = b"\"recorded_at_utc\":";
const TEMPERATURE_ONLY_JSON_FIELD_TEMPERATURE_C: &[u8] = b"\"temperature_c\":";

impl TelemetryV1TemperatureOnly {
    pub fn metamorphose_json(bytes: &[u8], max_response_bytes: usize) -> Result<Vec<u8>> {
        let archived = Self::access_archived(bytes)?;
        temperature_only_write_json_response(archived, max_response_bytes)
    }

    /// Metamorphoses immutable bytes already validated for this schema into JSON.
    ///
    /// # Safety
    /// The caller guarantees checked schema validation happened before immutable storage or transport.
    pub unsafe fn metamorphose_json_trusted_unchecked(
        bytes: &[u8],
        max_response_bytes: usize,
    ) -> Result<Vec<u8>> {
        let archived = unsafe { Self::access_archived_trusted_unchecked(bytes)? };
        temperature_only_write_json_response(archived, max_response_bytes)
    }
}

impl JsonMetamorphoseSchema for TelemetryV1TemperatureOnly {
    fn metamorphose_json(bytes: &[u8], max_response_bytes: usize) -> Result<Vec<u8>> {
        Self::metamorphose_json(bytes, max_response_bytes)
    }
    fn metamorphose_json_trusted_unchecked(
        bytes: &[u8],
        max_response_bytes: usize,
        _trusted: TrustedUnchecked,
    ) -> Result<Vec<u8>> {
        unsafe { Self::metamorphose_json_trusted_unchecked(bytes, max_response_bytes) }
    }
}

fn temperature_only_write_json_response(
    archived: &ArchivedTelemetryResponseV1PayloadTemperatureOnly,
    max_response_bytes: usize,
) -> Result<Vec<u8>> {
    let mut writer = JsonWriter::with_capacity(max_response_bytes, max_response_bytes.min(4096));
    writer.begin_object()?;
    writer.raw_static(TEMPERATURE_ONLY_JSON_SCHEMA_VERSION_FIELD)?;
    writer.u32_value(u32::from(SCHEMA_VERSION_VALUE))?;
    writer.comma()?;
    writer.raw_static(TEMPERATURE_ONLY_JSON_ROWS_FIELD)?;
    writer.begin_array()?;
    let mut first_row = true;
    for row in archived.rows.iter() {
        if first_row {
            first_row = false;
        } else {
            writer.comma()?;
        }
        temperature_only_write_json_row(row, &mut writer)?;
    }
    writer.end_array()?;
    writer.end_object()?;
    Ok(writer.finish())
}

fn temperature_only_write_json_row(
    row: &<TelemetryRowV1TemperatureOnly as Archive>::Archived,
    writer: &mut JsonWriter,
) -> Result<()> {
    writer.begin_object()?;
    let mut first = true;
    temperature_only_write_json_field_prefix(
        writer,
        &mut first,
        TEMPERATURE_ONLY_JSON_FIELD_SCHEMA_VERSION,
    )?;
    writer.u32_value(u32::from(1_u16))?;
    temperature_only_write_json_field_prefix(
        writer,
        &mut first,
        TEMPERATURE_ONLY_JSON_FIELD_DEVICE_ORDINAL,
    )?;
    writer.string_value(device_symbol(row.device_ordinal.to_native())?)?;
    temperature_only_write_json_field_prefix(
        writer,
        &mut first,
        TEMPERATURE_ONLY_JSON_FIELD_RECORDED_AT_MS,
    )?;
    writer.i64_value(row.recorded_at_ms.to_native())?;
    temperature_only_write_json_field_prefix(
        writer,
        &mut first,
        TEMPERATURE_ONLY_JSON_FIELD_RECORDED_AT_UTC,
    )?;
    writer.utc_value(row.recorded_at_ms.to_native())?;
    temperature_only_write_json_field_prefix(
        writer,
        &mut first,
        TEMPERATURE_ONLY_JSON_FIELD_TEMPERATURE_C,
    )?;
    writer.f64_value("temperature_c", row.temperature_c.to_native())?;
    writer.end_object()
}

fn temperature_only_write_json_field_prefix(
    writer: &mut JsonWriter,
    first: &mut bool,
    field: &'static [u8],
) -> Result<()> {
    if *first {
        *first = false;
    } else {
        writer.comma()?;
    }
    writer.raw_static(field)
}
