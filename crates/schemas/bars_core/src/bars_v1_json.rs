// This file is @generated by mbt_codegen.
// Do not edit by hand.
// schema_id=1 schema_version=1 schema_hash=6061383958499356843

use crate::bars_v1::*;
use mbt_adapter_json::JsonWriter;
use mbt_core::error::Result;
use mbt_metamorphose::{JsonMetamorphoseSchema, runtime::TrustedUnchecked};
use rkyv::Archive;

const JSON_SCHEMA_VERSION_FIELD: &[u8] = b"\"schema_version\":";
const JSON_ROWS_FIELD: &[u8] = b"\"rows\":";
const JSON_FIELD_SCHEMA_VERSION: &[u8] = b"\"schema_version\":";
const JSON_FIELD_PAIR_ORDINAL: &[u8] = b"\"pair\":";
const JSON_FIELD_TF_ORDINAL: &[u8] = b"\"tf\":";
const JSON_FIELD_OPEN_MS: &[u8] = b"\"open_ms\":";
const JSON_FIELD_CLOSE_MS: &[u8] = b"\"close_ms\":";
const JSON_FIELD_OPEN_UTC: &[u8] = b"\"open_utc\":";
const JSON_FIELD_CLOSE_UTC: &[u8] = b"\"close_utc\":";
const JSON_FIELD_O: &[u8] = b"\"o\":";
const JSON_FIELD_H: &[u8] = b"\"h\":";
const JSON_FIELD_L: &[u8] = b"\"l\":";
const JSON_FIELD_C: &[u8] = b"\"c\":";
const JSON_FIELD_V: &[u8] = b"\"v\":";
const JSON_FIELD_QUOTE_V: &[u8] = b"\"quote_v\":";
const JSON_FIELD_TAKER_KNOWN_V: &[u8] = b"\"taker_known_v\":";
const JSON_FIELD_TAKER_SIGNED_V: &[u8] = b"\"taker_signed_v\":";
const JSON_FIELD_TAKER_KNOWN_QUOTE_V: &[u8] = b"\"taker_known_quote_v\":";
const JSON_FIELD_TAKER_SIGNED_QUOTE_V: &[u8] = b"\"taker_signed_quote_v\":";
const JSON_FIELD_TAKER_KNOWN_N: &[u8] = b"\"taker_known_n\":";
const JSON_FIELD_TAKER_SIGNED_N: &[u8] = b"\"taker_signed_n\":";
const JSON_FIELD_VW: &[u8] = b"\"vw\":";
const JSON_FIELD_N: &[u8] = b"\"n\":";
const JSON_FIELD_SOURCE_ORDINAL: &[u8] = b"\"metadata.source\":";
const JSON_FIELD_PROCESS_ORDINAL: &[u8] = b"\"metadata.process\":";
const JSON_FIELD_VENUES_EXPECTED_MASK: &[u8] = b"\"metadata.venues_expected\":";
const JSON_FIELD_VENUES_WITH_TRADES_MASK: &[u8] = b"\"metadata.venues_with_trades\":";
const JSON_FIELD_INGESTED_AT_MS: &[u8] = b"\"metadata.ingested_at_ms\":";
const JSON_FIELD_INGESTED_AT_UTC: &[u8] = b"\"metadata.ingested_at_utc\":";
const JSON_FIELD_TARGET_INGESTED_AT_MS: &[u8] = b"\"metadata.target_ingested_at_ms\":";
const JSON_FIELD_TARGET_INGESTED_AT_UTC: &[u8] = b"\"metadata.target_ingested_at_utc\":";
const JSON_FIELD_BUILT_AT_MS: &[u8] = b"\"metadata.built_at_ms\":";
const JSON_FIELD_BUILT_AT_UTC: &[u8] = b"\"metadata.built_at_utc\":";
const JSON_FIELD_COMMITTED_AT_MS: &[u8] = b"\"metadata.committed_at_ms\":";
const JSON_FIELD_COMMITTED_AT_UTC: &[u8] = b"\"metadata.committed_at_utc\":";
const JSON_FIELD_HARMONIZED_AT_MS: &[u8] = b"\"metadata.harmonized_at_ms\":";
const JSON_FIELD_HARMONIZED_AT_UTC: &[u8] = b"\"metadata.harmonized_at_utc\":";
const JSON_FIELD_RECOMPUTED_AT_MS: &[u8] = b"\"metadata.recomputed_at_ms\":";
const JSON_FIELD_RECOMPUTED_AT_UTC: &[u8] = b"\"metadata.recomputed_at_utc\":";
const JSON_FIELD_RECOMPUTED_REASON_ORDINAL: &[u8] = b"\"metadata.recomputed_reason\":";
const JSON_FIELD_COVERED_1M_COUNT: &[u8] = b"\"metadata.covered_1m_count\":";
const JSON_FIELD_EXPECTED_1M_COUNT: &[u8] = b"\"metadata.expected_1m_count\":";
const JSON_FIELD_COVERAGE_RATIO: &[u8] = b"\"metadata.coverage_ratio\":";
const JSON_FIELD_INPUTS_SOURCE_COUNTS_FRONTIER: &[u8] =
    b"\"metadata.inputs_source_counts_frontier\":";
const JSON_FIELD_INPUTS_SOURCE_COUNTS_API: &[u8] = b"\"metadata.inputs_source_counts_api\":";
const JSON_FIELD_INPUTS_SOURCE_COUNTS_SYNTHETIC: &[u8] =
    b"\"metadata.inputs_source_counts_synthetic\":";
const JSON_FIELD_INPUTS_SOURCE_COUNTS_FIX_DATA: &[u8] =
    b"\"metadata.inputs_source_counts_fix_data\":";
const JSON_FIELD_FRONTIER_5S_INPUTS_COVERAGE_RATIO: &[u8] =
    b"\"metadata.frontier_5s_inputs_coverage_ratio\":";
const JSON_FIELD_FRONTIER_5S_EXPECTED: &[u8] = b"\"metadata.frontier_5s_expected\":";
const JSON_FIELD_FRONTIER_5S_SYNTH_N: &[u8] = b"\"metadata.frontier_5s_synth_n\":";
const JSON_FIELD_FRONTIER_5S_SYNTH_RATIO: &[u8] = b"\"metadata.frontier_5s_synth_ratio\":";
const JSON_FIELD_FRONTIER_5S_TRADE_N: &[u8] = b"\"metadata.frontier_5s_trade_n\":";
const JSON_FIELD_FRONTIER_5S_TRADE_RATIO: &[u8] = b"\"metadata.frontier_5s_trade_ratio\":";
const JSON_FIELD_AGE_MS: &[u8] = b"\"age_ms\":";

impl BarsV1 {
    pub fn metamorphose_json(bytes: &[u8], max_response_bytes: usize) -> Result<Vec<u8>> {
        let archived = Self::access_archived(bytes)?;
        write_json_response(archived, max_response_bytes)
    }

    /// Metamorphoses immutable bytes already validated for this schema into JSON.
    ///
    /// # Safety
    /// The caller guarantees checked schema validation happened before immutable storage or transport.
    pub unsafe fn metamorphose_json_trusted_unchecked(
        bytes: &[u8],
        max_response_bytes: usize,
    ) -> Result<Vec<u8>> {
        let archived = unsafe { Self::access_archived_trusted_unchecked(bytes)? };
        write_json_response(archived, max_response_bytes)
    }
}

impl JsonMetamorphoseSchema for BarsV1 {
    fn metamorphose_json(bytes: &[u8], max_response_bytes: usize) -> Result<Vec<u8>> {
        Self::metamorphose_json(bytes, max_response_bytes)
    }
    fn metamorphose_json_trusted_unchecked(
        bytes: &[u8],
        max_response_bytes: usize,
        _trusted: TrustedUnchecked,
    ) -> Result<Vec<u8>> {
        unsafe { Self::metamorphose_json_trusted_unchecked(bytes, max_response_bytes) }
    }
}

fn write_json_response(
    archived: &ArchivedMathildeTransportResponseV1Payload,
    max_response_bytes: usize,
) -> Result<Vec<u8>> {
    let mut writer = JsonWriter::with_capacity(max_response_bytes, max_response_bytes.min(4096));
    writer.begin_object()?;
    writer.raw_static(JSON_SCHEMA_VERSION_FIELD)?;
    writer.u32_value(u32::from(SCHEMA_VERSION_VALUE))?;
    writer.comma()?;
    writer.raw_static(JSON_ROWS_FIELD)?;
    writer.begin_array()?;
    let mut first_row = true;
    for row in archived.rows.iter() {
        if first_row {
            first_row = false;
        } else {
            writer.comma()?;
        }
        write_json_row(row, &mut writer)?;
    }
    writer.end_array()?;
    writer.end_object()?;
    Ok(writer.finish())
}

fn write_json_row(
    row: &<MathildeBarRowV1 as Archive>::Archived,
    writer: &mut JsonWriter,
) -> Result<()> {
    writer.begin_object()?;
    let mut first = true;
    write_json_field_prefix(writer, &mut first, JSON_FIELD_SCHEMA_VERSION)?;
    writer.u32_value(u32::from(1_u16))?;
    write_json_field_prefix(writer, &mut first, JSON_FIELD_PAIR_ORDINAL)?;
    writer.string_value(pair_symbol(row.pair_ordinal.to_native())?)?;
    write_json_field_prefix(writer, &mut first, JSON_FIELD_TF_ORDINAL)?;
    writer.string_value(tf_symbol(row.tf_ordinal.to_native())?)?;
    write_json_field_prefix(writer, &mut first, JSON_FIELD_OPEN_MS)?;
    writer.i64_value(row.open_ms.to_native())?;
    write_json_field_prefix(writer, &mut first, JSON_FIELD_CLOSE_MS)?;
    writer.i64_value(row.close_ms.to_native())?;
    write_json_field_prefix(writer, &mut first, JSON_FIELD_OPEN_UTC)?;
    writer.utc_value(row.open_ms.to_native())?;
    write_json_field_prefix(writer, &mut first, JSON_FIELD_CLOSE_UTC)?;
    writer.utc_value(row.close_ms.to_native())?;
    write_json_field_prefix(writer, &mut first, JSON_FIELD_O)?;
    writer.f64_value("o", row.o.to_native())?;
    write_json_field_prefix(writer, &mut first, JSON_FIELD_H)?;
    writer.f64_value("h", row.h.to_native())?;
    write_json_field_prefix(writer, &mut first, JSON_FIELD_L)?;
    writer.f64_value("l", row.l.to_native())?;
    write_json_field_prefix(writer, &mut first, JSON_FIELD_C)?;
    writer.f64_value("c", row.c.to_native())?;
    write_json_field_prefix(writer, &mut first, JSON_FIELD_V)?;
    writer.f64_value("v", row.v.to_native())?;
    write_json_field_prefix(writer, &mut first, JSON_FIELD_QUOTE_V)?;
    writer.f64_value("quote_v", row.quote_v.to_native())?;
    write_json_field_prefix(writer, &mut first, JSON_FIELD_TAKER_KNOWN_V)?;
    writer.f64_value("taker_known_v", row.taker_known_v.to_native())?;
    write_json_field_prefix(writer, &mut first, JSON_FIELD_TAKER_SIGNED_V)?;
    writer.f64_value("taker_signed_v", row.taker_signed_v.to_native())?;
    write_json_field_prefix(writer, &mut first, JSON_FIELD_TAKER_KNOWN_QUOTE_V)?;
    writer.f64_value("taker_known_quote_v", row.taker_known_quote_v.to_native())?;
    write_json_field_prefix(writer, &mut first, JSON_FIELD_TAKER_SIGNED_QUOTE_V)?;
    writer.f64_value("taker_signed_quote_v", row.taker_signed_quote_v.to_native())?;
    write_json_field_prefix(writer, &mut first, JSON_FIELD_TAKER_KNOWN_N)?;
    writer.i64_value(row.taker_known_n.to_native())?;
    write_json_field_prefix(writer, &mut first, JSON_FIELD_TAKER_SIGNED_N)?;
    writer.i64_value(row.taker_signed_n.to_native())?;
    if row.presence_bits.to_native() & PRESENCE_VW != 0 {
        write_json_field_prefix(writer, &mut first, JSON_FIELD_VW)?;
        writer.f64_value("vw", row.vw.to_native())?;
    }
    if row.presence_bits.to_native() & PRESENCE_N != 0 {
        write_json_field_prefix(writer, &mut first, JSON_FIELD_N)?;
        writer.i64_value(row.n.to_native())?;
    }
    write_json_field_prefix(writer, &mut first, JSON_FIELD_SOURCE_ORDINAL)?;
    writer.string_value(source_symbol(row.source_ordinal.to_native())?)?;
    if row.presence_bits.to_native() & PRESENCE_PROCESS_ORDINAL != 0 {
        write_json_field_prefix(writer, &mut first, JSON_FIELD_PROCESS_ORDINAL)?;
        writer.string_value(process_symbol(row.process_ordinal.to_native())?)?;
    }
    write_json_field_prefix(writer, &mut first, JSON_FIELD_VENUES_EXPECTED_MASK)?;
    write_json_venue_bitmask(row.venues_expected_mask.to_native(), writer)?;
    write_json_field_prefix(writer, &mut first, JSON_FIELD_VENUES_WITH_TRADES_MASK)?;
    write_json_venue_bitmask(row.venues_with_trades_mask.to_native(), writer)?;
    if row.presence_bits.to_native() & PRESENCE_INGESTED_AT_MS != 0 {
        write_json_field_prefix(writer, &mut first, JSON_FIELD_INGESTED_AT_MS)?;
        writer.i64_value(row.ingested_at_ms.to_native())?;
    }
    if row.presence_bits.to_native() & PRESENCE_INGESTED_AT_MS != 0 {
        write_json_field_prefix(writer, &mut first, JSON_FIELD_INGESTED_AT_UTC)?;
        writer.utc_value(row.ingested_at_ms.to_native())?;
    }
    if row.presence_bits.to_native() & PRESENCE_TARGET_INGESTED_AT_MS != 0 {
        write_json_field_prefix(writer, &mut first, JSON_FIELD_TARGET_INGESTED_AT_MS)?;
        writer.i64_value(row.target_ingested_at_ms.to_native())?;
    }
    if row.presence_bits.to_native() & PRESENCE_TARGET_INGESTED_AT_MS != 0 {
        write_json_field_prefix(writer, &mut first, JSON_FIELD_TARGET_INGESTED_AT_UTC)?;
        writer.utc_value(row.target_ingested_at_ms.to_native())?;
    }
    if row.presence_bits.to_native() & PRESENCE_BUILT_AT_MS != 0 {
        write_json_field_prefix(writer, &mut first, JSON_FIELD_BUILT_AT_MS)?;
        writer.i64_value(row.built_at_ms.to_native())?;
    }
    if row.presence_bits.to_native() & PRESENCE_BUILT_AT_MS != 0 {
        write_json_field_prefix(writer, &mut first, JSON_FIELD_BUILT_AT_UTC)?;
        writer.utc_value(row.built_at_ms.to_native())?;
    }
    if row.presence_bits.to_native() & PRESENCE_COMMITTED_AT_MS != 0 {
        write_json_field_prefix(writer, &mut first, JSON_FIELD_COMMITTED_AT_MS)?;
        writer.i64_value(row.committed_at_ms.to_native())?;
    }
    if row.presence_bits.to_native() & PRESENCE_COMMITTED_AT_MS != 0 {
        write_json_field_prefix(writer, &mut first, JSON_FIELD_COMMITTED_AT_UTC)?;
        writer.utc_value(row.committed_at_ms.to_native())?;
    }
    if row.presence_bits.to_native() & PRESENCE_HARMONIZED_AT_MS != 0 {
        write_json_field_prefix(writer, &mut first, JSON_FIELD_HARMONIZED_AT_MS)?;
        writer.i64_value(row.harmonized_at_ms.to_native())?;
    }
    if row.presence_bits.to_native() & PRESENCE_HARMONIZED_AT_MS != 0 {
        write_json_field_prefix(writer, &mut first, JSON_FIELD_HARMONIZED_AT_UTC)?;
        writer.utc_value(row.harmonized_at_ms.to_native())?;
    }
    if row.presence_bits.to_native() & PRESENCE_RECOMPUTED_AT_MS != 0 {
        write_json_field_prefix(writer, &mut first, JSON_FIELD_RECOMPUTED_AT_MS)?;
        writer.i64_value(row.recomputed_at_ms.to_native())?;
    }
    if row.presence_bits.to_native() & PRESENCE_RECOMPUTED_AT_MS != 0 {
        write_json_field_prefix(writer, &mut first, JSON_FIELD_RECOMPUTED_AT_UTC)?;
        writer.utc_value(row.recomputed_at_ms.to_native())?;
    }
    if row.presence_bits.to_native() & PRESENCE_RECOMPUTED_REASON_ORDINAL != 0 {
        write_json_field_prefix(writer, &mut first, JSON_FIELD_RECOMPUTED_REASON_ORDINAL)?;
        writer.string_value(recomputed_reason_symbol(
            row.recomputed_reason_ordinal.to_native(),
        )?)?;
    }
    if row.presence_bits.to_native() & PRESENCE_COVERED_1M_COUNT != 0 {
        write_json_field_prefix(writer, &mut first, JSON_FIELD_COVERED_1M_COUNT)?;
        writer.i64_value(row.covered_1m_count.to_native())?;
    }
    if row.presence_bits.to_native() & PRESENCE_EXPECTED_1M_COUNT != 0 {
        write_json_field_prefix(writer, &mut first, JSON_FIELD_EXPECTED_1M_COUNT)?;
        writer.i64_value(row.expected_1m_count.to_native())?;
    }
    if row.presence_bits.to_native() & PRESENCE_COVERAGE_RATIO != 0 {
        write_json_field_prefix(writer, &mut first, JSON_FIELD_COVERAGE_RATIO)?;
        writer.f64_value("metadata.coverage_ratio", row.coverage_ratio.to_native())?;
    }
    if row.presence_bits.to_native() & PRESENCE_INPUTS_SOURCE_COUNTS_FRONTIER != 0 {
        write_json_field_prefix(writer, &mut first, JSON_FIELD_INPUTS_SOURCE_COUNTS_FRONTIER)?;
        writer.i64_value(row.inputs_source_counts_frontier.to_native())?;
    }
    if row.presence_bits.to_native() & PRESENCE_INPUTS_SOURCE_COUNTS_API != 0 {
        write_json_field_prefix(writer, &mut first, JSON_FIELD_INPUTS_SOURCE_COUNTS_API)?;
        writer.i64_value(row.inputs_source_counts_api.to_native())?;
    }
    if row.presence_bits.to_native() & PRESENCE_INPUTS_SOURCE_COUNTS_SYNTHETIC != 0 {
        write_json_field_prefix(
            writer,
            &mut first,
            JSON_FIELD_INPUTS_SOURCE_COUNTS_SYNTHETIC,
        )?;
        writer.i64_value(row.inputs_source_counts_synthetic.to_native())?;
    }
    if row.presence_bits.to_native() & PRESENCE_INPUTS_SOURCE_COUNTS_FIX_DATA != 0 {
        write_json_field_prefix(writer, &mut first, JSON_FIELD_INPUTS_SOURCE_COUNTS_FIX_DATA)?;
        writer.i64_value(row.inputs_source_counts_fix_data.to_native())?;
    }
    if row.presence_bits.to_native() & PRESENCE_FRONTIER_5S_INPUTS_COVERAGE_RATIO != 0 {
        write_json_field_prefix(
            writer,
            &mut first,
            JSON_FIELD_FRONTIER_5S_INPUTS_COVERAGE_RATIO,
        )?;
        writer.f64_value(
            "metadata.frontier_5s_inputs_coverage_ratio",
            row.frontier_5s_inputs_coverage_ratio.to_native(),
        )?;
    }
    if row.presence_bits.to_native() & PRESENCE_FRONTIER_5S_EXPECTED != 0 {
        write_json_field_prefix(writer, &mut first, JSON_FIELD_FRONTIER_5S_EXPECTED)?;
        writer.i64_value(row.frontier_5s_expected.to_native())?;
    }
    if row.presence_bits.to_native() & PRESENCE_FRONTIER_5S_SYNTH_N != 0 {
        write_json_field_prefix(writer, &mut first, JSON_FIELD_FRONTIER_5S_SYNTH_N)?;
        writer.i64_value(row.frontier_5s_synth_n.to_native())?;
    }
    if row.presence_bits.to_native() & PRESENCE_FRONTIER_5S_SYNTH_RATIO != 0 {
        write_json_field_prefix(writer, &mut first, JSON_FIELD_FRONTIER_5S_SYNTH_RATIO)?;
        writer.f64_value(
            "metadata.frontier_5s_synth_ratio",
            row.frontier_5s_synth_ratio.to_native(),
        )?;
    }
    if row.presence_bits.to_native() & PRESENCE_FRONTIER_5S_TRADE_N != 0 {
        write_json_field_prefix(writer, &mut first, JSON_FIELD_FRONTIER_5S_TRADE_N)?;
        writer.i64_value(row.frontier_5s_trade_n.to_native())?;
    }
    if row.presence_bits.to_native() & PRESENCE_FRONTIER_5S_TRADE_RATIO != 0 {
        write_json_field_prefix(writer, &mut first, JSON_FIELD_FRONTIER_5S_TRADE_RATIO)?;
        writer.f64_value(
            "metadata.frontier_5s_trade_ratio",
            row.frontier_5s_trade_ratio.to_native(),
        )?;
    }
    if row.presence_bits.to_native() & PRESENCE_AGE_MS != 0 {
        write_json_field_prefix(writer, &mut first, JSON_FIELD_AGE_MS)?;
        writer.i64_value(row.age_ms.to_native())?;
    }
    writer.end_object()
}

fn write_json_venue_bitmask(mask: u64, writer: &mut JsonWriter) -> Result<()> {
    writer.begin_array()?;
    let mut first = true;
    if mask & (1_u64 << 0) != 0 {
        if first {
            first = false;
        } else {
            writer.comma()?;
        }
        writer.string_value("binance")?;
    }
    if mask & (1_u64 << 1) != 0 {
        if first {
            first = false;
        } else {
            writer.comma()?;
        }
        writer.string_value("bybit")?;
    }
    if mask & (1_u64 << 2) != 0 {
        if first {
            first = false;
        } else {
            writer.comma()?;
        }
        writer.string_value("okx")?;
    }
    let _ = first;
    writer.end_array()
}

fn write_json_field_prefix(
    writer: &mut JsonWriter,
    first: &mut bool,
    field: &'static [u8],
) -> Result<()> {
    if *first {
        *first = false;
    } else {
        writer.comma()?;
    }
    writer.raw_static(field)
}
