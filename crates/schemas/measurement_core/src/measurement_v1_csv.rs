// This file is @generated by mbt_codegen.
// Do not edit by hand.
// schema_id=50002 schema_version=1 schema_hash=9409304714461393349

use crate::measurement_v1::*;
use mbt_adapter_csv::CsvWriter;
use mbt_core::error::Result;
use mbt_metamorphose::{CsvMetamorphoseSchema, runtime::TrustedUnchecked};
use rkyv::Archive;

const CSV_HEADER: &[u8] = b"schema_version,device,interval,started_at_ms,recorded_at_ms,started_at_utc,recorded_at_utc,value_a,value_b,value_c,value_d,value_e,value_f,value_g,value_h,value_i,value_j,count_a,count_b,average_value,sample_count,metadata.source,metadata.process,metadata.sites_expected,metadata.sites_observed,metadata.ingested_at_ms,metadata.ingested_at_utc,metadata.target_ingested_at_ms,metadata.target_ingested_at_utc,metadata.built_at_ms,metadata.built_at_utc,metadata.committed_at_ms,metadata.committed_at_utc,metadata.harmonized_at_ms,metadata.harmonized_at_utc,metadata.recomputed_at_ms,metadata.recomputed_at_utc,metadata.recomputed_reason,metadata.covered_interval_count,metadata.expected_interval_count,metadata.coverage_ratio,metadata.inputs_source_counts_sensor,metadata.inputs_source_counts_api,metadata.inputs_source_counts_synthetic,metadata.inputs_source_counts_corrected,metadata.sensor_window_inputs_coverage_ratio,metadata.sensor_window_expected,metadata.sensor_window_synthetic_count,metadata.sensor_window_synthetic_ratio,metadata.sensor_window_observed_count,metadata.sensor_window_observed_ratio,age_ms";

impl MeasurementV1 {
    pub fn metamorphose_csv(bytes: &[u8], max_response_bytes: usize) -> Result<Vec<u8>> {
        let archived = Self::access_archived(bytes)?;
        write_csv_response(archived, max_response_bytes)
    }

    /// Metamorphoses immutable bytes already validated for this schema into CSV.
    ///
    /// # Safety
    /// The caller guarantees checked schema validation happened before immutable storage or transport.
    pub unsafe fn metamorphose_csv_trusted_unchecked(
        bytes: &[u8],
        max_response_bytes: usize,
    ) -> Result<Vec<u8>> {
        let archived = unsafe { Self::access_archived_trusted_unchecked(bytes)? };
        write_csv_response(archived, max_response_bytes)
    }
}

impl CsvMetamorphoseSchema for MeasurementV1 {
    fn metamorphose_csv(bytes: &[u8], max_response_bytes: usize) -> Result<Vec<u8>> {
        Self::metamorphose_csv(bytes, max_response_bytes)
    }
    fn metamorphose_csv_trusted_unchecked(
        bytes: &[u8],
        max_response_bytes: usize,
        _trusted: TrustedUnchecked,
    ) -> Result<Vec<u8>> {
        unsafe { Self::metamorphose_csv_trusted_unchecked(bytes, max_response_bytes) }
    }
}

fn write_csv_response(
    archived: &ArchivedMeasurementResponseV1Payload,
    max_response_bytes: usize,
) -> Result<Vec<u8>> {
    let mut writer = CsvWriter::with_capacity(max_response_bytes, max_response_bytes.min(4096));
    writer.raw_static(CSV_HEADER)?;
    writer.newline()?;
    for row in archived.rows.iter() {
        write_csv_row(row, &mut writer)?;
    }
    Ok(writer.finish())
}

fn write_csv_row(
    row: &<MeasurementRowV1 as Archive>::Archived,
    writer: &mut CsvWriter,
) -> Result<()> {
    writer.u32_cell(u32::from(1_u16))?;
    writer.comma()?;
    writer.string_cell(device_symbol(row.device_ordinal.to_native())?)?;
    writer.comma()?;
    writer.string_cell(interval_symbol(row.interval_ordinal.to_native())?)?;
    writer.comma()?;
    writer.i64_cell(row.started_at_ms.to_native())?;
    writer.comma()?;
    writer.i64_cell(row.recorded_at_ms.to_native())?;
    writer.comma()?;
    writer.utc_cell(row.started_at_ms.to_native())?;
    writer.comma()?;
    writer.utc_cell(row.recorded_at_ms.to_native())?;
    writer.comma()?;
    writer.f64_cell("value_a", row.value_a.to_native())?;
    writer.comma()?;
    writer.f64_cell("value_b", row.value_b.to_native())?;
    writer.comma()?;
    writer.f64_cell("value_c", row.value_c.to_native())?;
    writer.comma()?;
    writer.f64_cell("value_d", row.value_d.to_native())?;
    writer.comma()?;
    writer.f64_cell("value_e", row.value_e.to_native())?;
    writer.comma()?;
    writer.f64_cell("value_f", row.value_f.to_native())?;
    writer.comma()?;
    writer.f64_cell("value_g", row.value_g.to_native())?;
    writer.comma()?;
    writer.f64_cell("value_h", row.value_h.to_native())?;
    writer.comma()?;
    writer.f64_cell("value_i", row.value_i.to_native())?;
    writer.comma()?;
    writer.f64_cell("value_j", row.value_j.to_native())?;
    writer.comma()?;
    writer.i64_cell(row.count_a.to_native())?;
    writer.comma()?;
    writer.i64_cell(row.count_b.to_native())?;
    writer.comma()?;
    if row.presence_bits.to_native() & PRESENCE_AVERAGE_VALUE != 0 {
        writer.f64_cell("average_value", row.average_value.to_native())?;
    }
    writer.comma()?;
    if row.presence_bits.to_native() & PRESENCE_SAMPLE_COUNT != 0 {
        writer.i64_cell(row.sample_count.to_native())?;
    }
    writer.comma()?;
    writer.string_cell(source_symbol(row.source_ordinal.to_native())?)?;
    writer.comma()?;
    if row.presence_bits.to_native() & PRESENCE_PROCESS_ORDINAL != 0 {
        writer.string_cell(process_symbol(row.process_ordinal.to_native())?)?;
    }
    writer.comma()?;
    write_csv_site_bitmask(row.sites_expected_mask.to_native(), writer)?;
    writer.comma()?;
    write_csv_site_bitmask(row.sites_observed_mask.to_native(), writer)?;
    writer.comma()?;
    if row.presence_bits.to_native() & PRESENCE_INGESTED_AT_MS != 0 {
        writer.i64_cell(row.ingested_at_ms.to_native())?;
    }
    writer.comma()?;
    if row.presence_bits.to_native() & PRESENCE_INGESTED_AT_MS != 0 {
        writer.utc_cell(row.ingested_at_ms.to_native())?;
    }
    writer.comma()?;
    if row.presence_bits.to_native() & PRESENCE_TARGET_INGESTED_AT_MS != 0 {
        writer.i64_cell(row.target_ingested_at_ms.to_native())?;
    }
    writer.comma()?;
    if row.presence_bits.to_native() & PRESENCE_TARGET_INGESTED_AT_MS != 0 {
        writer.utc_cell(row.target_ingested_at_ms.to_native())?;
    }
    writer.comma()?;
    if row.presence_bits.to_native() & PRESENCE_BUILT_AT_MS != 0 {
        writer.i64_cell(row.built_at_ms.to_native())?;
    }
    writer.comma()?;
    if row.presence_bits.to_native() & PRESENCE_BUILT_AT_MS != 0 {
        writer.utc_cell(row.built_at_ms.to_native())?;
    }
    writer.comma()?;
    if row.presence_bits.to_native() & PRESENCE_COMMITTED_AT_MS != 0 {
        writer.i64_cell(row.committed_at_ms.to_native())?;
    }
    writer.comma()?;
    if row.presence_bits.to_native() & PRESENCE_COMMITTED_AT_MS != 0 {
        writer.utc_cell(row.committed_at_ms.to_native())?;
    }
    writer.comma()?;
    if row.presence_bits.to_native() & PRESENCE_HARMONIZED_AT_MS != 0 {
        writer.i64_cell(row.harmonized_at_ms.to_native())?;
    }
    writer.comma()?;
    if row.presence_bits.to_native() & PRESENCE_HARMONIZED_AT_MS != 0 {
        writer.utc_cell(row.harmonized_at_ms.to_native())?;
    }
    writer.comma()?;
    if row.presence_bits.to_native() & PRESENCE_RECOMPUTED_AT_MS != 0 {
        writer.i64_cell(row.recomputed_at_ms.to_native())?;
    }
    writer.comma()?;
    if row.presence_bits.to_native() & PRESENCE_RECOMPUTED_AT_MS != 0 {
        writer.utc_cell(row.recomputed_at_ms.to_native())?;
    }
    writer.comma()?;
    if row.presence_bits.to_native() & PRESENCE_RECOMPUTED_REASON_ORDINAL != 0 {
        writer.string_cell(recomputed_reason_symbol(
            row.recomputed_reason_ordinal.to_native(),
        )?)?;
    }
    writer.comma()?;
    if row.presence_bits.to_native() & PRESENCE_COVERED_INTERVAL_COUNT != 0 {
        writer.i64_cell(row.covered_interval_count.to_native())?;
    }
    writer.comma()?;
    if row.presence_bits.to_native() & PRESENCE_EXPECTED_INTERVAL_COUNT != 0 {
        writer.i64_cell(row.expected_interval_count.to_native())?;
    }
    writer.comma()?;
    if row.presence_bits.to_native() & PRESENCE_COVERAGE_RATIO != 0 {
        writer.f64_cell("metadata.coverage_ratio", row.coverage_ratio.to_native())?;
    }
    writer.comma()?;
    if row.presence_bits.to_native() & PRESENCE_INPUTS_SOURCE_COUNTS_SENSOR != 0 {
        writer.i64_cell(row.inputs_source_counts_sensor.to_native())?;
    }
    writer.comma()?;
    if row.presence_bits.to_native() & PRESENCE_INPUTS_SOURCE_COUNTS_API != 0 {
        writer.i64_cell(row.inputs_source_counts_api.to_native())?;
    }
    writer.comma()?;
    if row.presence_bits.to_native() & PRESENCE_INPUTS_SOURCE_COUNTS_SYNTHETIC != 0 {
        writer.i64_cell(row.inputs_source_counts_synthetic.to_native())?;
    }
    writer.comma()?;
    if row.presence_bits.to_native() & PRESENCE_INPUTS_SOURCE_COUNTS_CORRECTED != 0 {
        writer.i64_cell(row.inputs_source_counts_corrected.to_native())?;
    }
    writer.comma()?;
    if row.presence_bits.to_native() & PRESENCE_SENSOR_WINDOW_INPUTS_COVERAGE_RATIO != 0 {
        writer.f64_cell(
            "metadata.sensor_window_inputs_coverage_ratio",
            row.sensor_window_inputs_coverage_ratio.to_native(),
        )?;
    }
    writer.comma()?;
    if row.presence_bits.to_native() & PRESENCE_SENSOR_WINDOW_EXPECTED != 0 {
        writer.i64_cell(row.sensor_window_expected.to_native())?;
    }
    writer.comma()?;
    if row.presence_bits.to_native() & PRESENCE_SENSOR_WINDOW_SYNTHETIC_COUNT != 0 {
        writer.i64_cell(row.sensor_window_synthetic_count.to_native())?;
    }
    writer.comma()?;
    if row.presence_bits.to_native() & PRESENCE_SENSOR_WINDOW_SYNTHETIC_RATIO != 0 {
        writer.f64_cell(
            "metadata.sensor_window_synthetic_ratio",
            row.sensor_window_synthetic_ratio.to_native(),
        )?;
    }
    writer.comma()?;
    if row.presence_bits.to_native() & PRESENCE_SENSOR_WINDOW_OBSERVED_COUNT != 0 {
        writer.i64_cell(row.sensor_window_observed_count.to_native())?;
    }
    writer.comma()?;
    if row.presence_bits.to_native() & PRESENCE_SENSOR_WINDOW_OBSERVED_RATIO != 0 {
        writer.f64_cell(
            "metadata.sensor_window_observed_ratio",
            row.sensor_window_observed_ratio.to_native(),
        )?;
    }
    writer.comma()?;
    if row.presence_bits.to_native() & PRESENCE_AGE_MS != 0 {
        writer.i64_cell(row.age_ms.to_native())?;
    }
    writer.newline()
}

fn write_csv_site_bitmask(mask: u64, writer: &mut CsvWriter) -> Result<()> {
    writer.begin_array_cell()?;
    let mut first = true;
    if mask & (1_u64 << 0) != 0 {
        if first {
            first = false;
        } else {
            writer.array_cell_comma()?;
        }
        writer.array_string_cell("site_a")?;
    }
    if mask & (1_u64 << 1) != 0 {
        if first {
            first = false;
        } else {
            writer.array_cell_comma()?;
        }
        writer.array_string_cell("site_b")?;
    }
    if mask & (1_u64 << 2) != 0 {
        if first {
            first = false;
        } else {
            writer.array_cell_comma()?;
        }
        writer.array_string_cell("site_c")?;
    }
    let _ = first;
    writer.end_array_cell()
}

const WITHOUT_DETAILS_PRESENCE_AVERAGE_VALUE: u64 = 1 << 0;
const WITHOUT_DETAILS_PRESENCE_SAMPLE_COUNT: u64 = 1 << 1;
const WITHOUT_DETAILS_PRESENCE_AGE_MS: u64 = 1 << 2;

const WITHOUT_DETAILS_CSV_HEADER: &[u8] = b"schema_version,device,interval,started_at_ms,recorded_at_ms,started_at_utc,recorded_at_utc,value_a,value_b,value_c,value_d,value_e,value_f,value_g,value_h,value_i,value_j,count_a,count_b,average_value,sample_count,age_ms";

impl MeasurementV1WithoutDetails {
    pub fn metamorphose_csv(bytes: &[u8], max_response_bytes: usize) -> Result<Vec<u8>> {
        let archived = Self::access_archived(bytes)?;
        without_details_write_csv_response(archived, max_response_bytes)
    }

    /// Metamorphoses immutable bytes already validated for this schema into CSV.
    ///
    /// # Safety
    /// The caller guarantees checked schema validation happened before immutable storage or transport.
    pub unsafe fn metamorphose_csv_trusted_unchecked(
        bytes: &[u8],
        max_response_bytes: usize,
    ) -> Result<Vec<u8>> {
        let archived = unsafe { Self::access_archived_trusted_unchecked(bytes)? };
        without_details_write_csv_response(archived, max_response_bytes)
    }
}

impl CsvMetamorphoseSchema for MeasurementV1WithoutDetails {
    fn metamorphose_csv(bytes: &[u8], max_response_bytes: usize) -> Result<Vec<u8>> {
        Self::metamorphose_csv(bytes, max_response_bytes)
    }
    fn metamorphose_csv_trusted_unchecked(
        bytes: &[u8],
        max_response_bytes: usize,
        _trusted: TrustedUnchecked,
    ) -> Result<Vec<u8>> {
        unsafe { Self::metamorphose_csv_trusted_unchecked(bytes, max_response_bytes) }
    }
}

fn without_details_write_csv_response(
    archived: &ArchivedMeasurementResponseV1PayloadWithoutDetails,
    max_response_bytes: usize,
) -> Result<Vec<u8>> {
    let mut writer = CsvWriter::with_capacity(max_response_bytes, max_response_bytes.min(4096));
    writer.raw_static(WITHOUT_DETAILS_CSV_HEADER)?;
    writer.newline()?;
    for row in archived.rows.iter() {
        without_details_write_csv_row(row, &mut writer)?;
    }
    Ok(writer.finish())
}

fn without_details_write_csv_row(
    row: &<MeasurementRowV1WithoutDetails as Archive>::Archived,
    writer: &mut CsvWriter,
) -> Result<()> {
    writer.u32_cell(u32::from(1_u16))?;
    writer.comma()?;
    writer.string_cell(device_symbol(row.device_ordinal.to_native())?)?;
    writer.comma()?;
    writer.string_cell(interval_symbol(row.interval_ordinal.to_native())?)?;
    writer.comma()?;
    writer.i64_cell(row.started_at_ms.to_native())?;
    writer.comma()?;
    writer.i64_cell(row.recorded_at_ms.to_native())?;
    writer.comma()?;
    writer.utc_cell(row.started_at_ms.to_native())?;
    writer.comma()?;
    writer.utc_cell(row.recorded_at_ms.to_native())?;
    writer.comma()?;
    writer.f64_cell("value_a", row.value_a.to_native())?;
    writer.comma()?;
    writer.f64_cell("value_b", row.value_b.to_native())?;
    writer.comma()?;
    writer.f64_cell("value_c", row.value_c.to_native())?;
    writer.comma()?;
    writer.f64_cell("value_d", row.value_d.to_native())?;
    writer.comma()?;
    writer.f64_cell("value_e", row.value_e.to_native())?;
    writer.comma()?;
    writer.f64_cell("value_f", row.value_f.to_native())?;
    writer.comma()?;
    writer.f64_cell("value_g", row.value_g.to_native())?;
    writer.comma()?;
    writer.f64_cell("value_h", row.value_h.to_native())?;
    writer.comma()?;
    writer.f64_cell("value_i", row.value_i.to_native())?;
    writer.comma()?;
    writer.f64_cell("value_j", row.value_j.to_native())?;
    writer.comma()?;
    writer.i64_cell(row.count_a.to_native())?;
    writer.comma()?;
    writer.i64_cell(row.count_b.to_native())?;
    writer.comma()?;
    if row.presence_bits.to_native() & WITHOUT_DETAILS_PRESENCE_AVERAGE_VALUE != 0 {
        writer.f64_cell("average_value", row.average_value.to_native())?;
    }
    writer.comma()?;
    if row.presence_bits.to_native() & WITHOUT_DETAILS_PRESENCE_SAMPLE_COUNT != 0 {
        writer.i64_cell(row.sample_count.to_native())?;
    }
    writer.comma()?;
    if row.presence_bits.to_native() & WITHOUT_DETAILS_PRESENCE_AGE_MS != 0 {
        writer.i64_cell(row.age_ms.to_native())?;
    }
    writer.newline()
}

const VALUES_ONLY_CSV_HEADER: &[u8] = b"schema_version,device,interval,started_at_ms,recorded_at_ms,started_at_utc,recorded_at_utc,value_a,value_b,value_c,value_d,value_e";

impl MeasurementV1ValuesOnly {
    pub fn metamorphose_csv(bytes: &[u8], max_response_bytes: usize) -> Result<Vec<u8>> {
        let archived = Self::access_archived(bytes)?;
        values_only_write_csv_response(archived, max_response_bytes)
    }

    /// Metamorphoses immutable bytes already validated for this schema into CSV.
    ///
    /// # Safety
    /// The caller guarantees checked schema validation happened before immutable storage or transport.
    pub unsafe fn metamorphose_csv_trusted_unchecked(
        bytes: &[u8],
        max_response_bytes: usize,
    ) -> Result<Vec<u8>> {
        let archived = unsafe { Self::access_archived_trusted_unchecked(bytes)? };
        values_only_write_csv_response(archived, max_response_bytes)
    }
}

impl CsvMetamorphoseSchema for MeasurementV1ValuesOnly {
    fn metamorphose_csv(bytes: &[u8], max_response_bytes: usize) -> Result<Vec<u8>> {
        Self::metamorphose_csv(bytes, max_response_bytes)
    }
    fn metamorphose_csv_trusted_unchecked(
        bytes: &[u8],
        max_response_bytes: usize,
        _trusted: TrustedUnchecked,
    ) -> Result<Vec<u8>> {
        unsafe { Self::metamorphose_csv_trusted_unchecked(bytes, max_response_bytes) }
    }
}

fn values_only_write_csv_response(
    archived: &ArchivedMeasurementResponseV1PayloadValuesOnly,
    max_response_bytes: usize,
) -> Result<Vec<u8>> {
    let mut writer = CsvWriter::with_capacity(max_response_bytes, max_response_bytes.min(4096));
    writer.raw_static(VALUES_ONLY_CSV_HEADER)?;
    writer.newline()?;
    for row in archived.rows.iter() {
        values_only_write_csv_row(row, &mut writer)?;
    }
    Ok(writer.finish())
}

fn values_only_write_csv_row(
    row: &<MeasurementRowV1ValuesOnly as Archive>::Archived,
    writer: &mut CsvWriter,
) -> Result<()> {
    writer.u32_cell(u32::from(1_u16))?;
    writer.comma()?;
    writer.string_cell(device_symbol(row.device_ordinal.to_native())?)?;
    writer.comma()?;
    writer.string_cell(interval_symbol(row.interval_ordinal.to_native())?)?;
    writer.comma()?;
    writer.i64_cell(row.started_at_ms.to_native())?;
    writer.comma()?;
    writer.i64_cell(row.recorded_at_ms.to_native())?;
    writer.comma()?;
    writer.utc_cell(row.started_at_ms.to_native())?;
    writer.comma()?;
    writer.utc_cell(row.recorded_at_ms.to_native())?;
    writer.comma()?;
    writer.f64_cell("value_a", row.value_a.to_native())?;
    writer.comma()?;
    writer.f64_cell("value_b", row.value_b.to_native())?;
    writer.comma()?;
    writer.f64_cell("value_c", row.value_c.to_native())?;
    writer.comma()?;
    writer.f64_cell("value_d", row.value_d.to_native())?;
    writer.comma()?;
    writer.f64_cell("value_e", row.value_e.to_native())?;
    writer.newline()
}
